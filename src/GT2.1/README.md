# GT2.1 - React + TypeScript Components

This folder contains a simple React component example using TypeScript.

It demonstrates:
- typed props with interfaces
- a functional component with React.FC
- a reusable card component
- simple component composition

Run it with a React project setup such as:

```bash
npm start
```
