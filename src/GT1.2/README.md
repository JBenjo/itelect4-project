# GT1.2 - Advanced Types in TypeScript

This folder contains a TypeScript example for:
- interfaces
- type aliases
- union types
- intersection types
- type narrowing
- basic generics concepts

Run the file with:

```bash
npx ts-node src/GT1.2/index.ts
```
